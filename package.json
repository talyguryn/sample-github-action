{
  "name": "sample-github-action",
  "scripts": {
    "build": "ncc build index.js --minify"
  },
  "devDependencies": {
    "@actions/core": "^1.2.6",
    "@vercel/ncc": "^0.25.1",
    "node-whois": "^2.1.3",
    "parse-whois": "^1.0.0"
  }
}
